@{
    Layout = "_ColumnsOne";

    //title
    Html.AddTitleParts(T("PageTitle.Checkout").Text);
    //page class
    Html.AppendPageCssClassParts("html-checkout-page");
    Html.AppendPageCssClassParts("html-order-completed-page");
}
<div class="page checkout-page order-completed-page">
    <div class="page-body checkout-data">
        <div class="section order-completed alert alert-warning text-center buttons">
            <div class="details">
                <div class="order-number">
                    <strong>@ViewBag.PaymentMessage</strong>
                </div>
            </div>
            @if (ViewBag.RePayId != null)
            {
                @*<form asp-route="OrderDetails" asp-route-orderId="@ViewBag.RePayId" method="post">
                        <input type="submit" name="repost-payment" value="@T("Order.RetryPayment")" class="button-1 btn btn-primary" />
                        <br />
                        <p class="hint">
                            <em>@T("Order.RetryPayment.Hint")</em>
                        </p>
                    </form>*@
                <a href="@Url.RouteUrl("OrderDetails", new { orderID = ViewBag.RePayId })" class="button-1 btn btn-primary">@T("Checkout.PlacedOrderDetails")</a>
            }
        </div>
    </div>
</div>
